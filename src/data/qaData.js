export const QA_SEED = [
  { q: 'What is the difference between var, let, and const?', cat: 'js', diff: 'easy', src: 'NamasteDev / Every JS interview', a: 'var is function-scoped and hoisted (initialized as undefined). let and const are block-scoped and in the TDZ (temporal dead zone) until their declaration — accessing before declaration throws ReferenceError. const cannot be reassigned but its object/array contents can mutate.' },
  { q: 'What is a closure? Give a real-world example.', cat: 'js', diff: 'easy', src: 'Common FE interview', a: 'A closure is when a function remembers the variables from its outer scope even after that outer function has returned. Real world: makeCounter() — inner functions (increment, decrement) close over the count variable. Used in debounce, memoization, module pattern.' },
  { q: 'Explain the JavaScript event loop — what are macro and microtasks?', cat: 'js', diff: 'medium', src: 'Product company JS round', a: 'The call stack runs synchronous code. When async ops complete, their callbacks go into queues. Microtasks (Promise.then, queueMicrotask) are processed after each task, before the next macro-task. Macrotasks (setTimeout, setInterval, I/O) run one at a time. So: call stack empties → all microtasks run → one macrotask → all microtasks → one macrotask...' },
  { q: 'What is reconciliation in React?', cat: 'react', diff: 'medium', src: 'React interview — almost universal', a: "React builds a virtual DOM tree. When state changes, it builds a new virtual tree and diffs it against the previous one (Fiber algorithm). Only the actual differences are applied to the real DOM. Key insight: if the component type changes, React unmounts and remounts entirely. If it's the same type, it updates in-place." },
  { q: 'When would you use useCallback and useMemo? When NOT to?', cat: 'react', diff: 'medium', src: 'Senior React interview', a: "useMemo caches a computed value, useCallback caches a function reference. Use when: passing callbacks to deeply optimized child components (React.memo), expensive computations, dependency arrays in useEffect that would otherwise change reference every render. NOT when: the computation is cheap, the component isn't actually re-rendering unnecessarily. Every memoization has overhead — don't add it by default." },
  { q: 'Solve Two Sum — what is the hashmap approach and why is it O(n)?', cat: 'dsa', diff: 'easy', src: 'Your actual failed interview — own this forever', a: 'For each number, check if (target - num) exists in a map. If yes, return [map[complement], current index]. If no, add num → index to the map. O(n) because we visit each element once and hash lookup is O(1). The brute force nested loop is O(n²). Code: const map = {}; for(let i=0;i<nums.length;i++){ const comp = target - nums[i]; if(map[comp] !== undefined) return [map[comp], i]; map[nums[i]] = i; }' },
  { q: 'What is the difference between controlled and uncontrolled components in React?', cat: 'react', diff: 'easy', src: 'React fundamentals round', a: 'Controlled: React state is the single source of truth. Input value is set via state, changes go through onChange → setState. You always know the value. Uncontrolled: the DOM manages its own state, you access it via a ref. Simpler for simple forms, but harder to validate or sync. For most product work, use controlled.' },
  { q: 'Write debounce from scratch.', cat: 'js', diff: 'hard', src: 'Product company FE round — Swiggy, Meesho level', a: 'function debounce(fn, delay) { let timer; return function(...args) { clearTimeout(timer); timer = setTimeout(() => fn.apply(this, args), delay); }; } — The key: every call clears the previous timer and sets a new one. Only fires when the user stops triggering it for `delay` ms.' },
  { q: 'What is CSS specificity and how is it calculated?', cat: 'css', diff: 'easy', src: 'FE fundamentals', a: 'Specificity determines which CSS rule wins when multiple rules match. Calculated as: inline style (1,0,0,0) > ID selector (0,1,0,0) > class/attribute/pseudo-class (0,0,1,0) > element/pseudo-element (0,0,0,1). !important overrides all. Higher specificity wins; equal specificity, last rule wins.' },
  { q: 'How does useEffect cleanup work and when do you need it?', cat: 'react', diff: 'medium', src: 'React hooks deep dive round', a: 'The function returned from useEffect runs before the component unmounts AND before the effect runs again (if deps changed). Use it to cancel subscriptions, clear timers, abort fetch requests, remove event listeners. Common mistake: not cleaning up intervals or WebSocket connections — causes memory leaks in React apps.' },
];
